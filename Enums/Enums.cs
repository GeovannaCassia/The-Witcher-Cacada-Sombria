namespace TheWitcher.Enums
{
    // Enum para as classes disponíveis
    public enum ClassePersonagem
    {
        Guerreiro,
        Mago,
        Arqueiro
    }

    // Enum para as raças disponíveis
    public enum RacaPersonagem
    {
        Humano,
        Elfo,
        Anao
    }

    // Enum para o status de uma missão
    public enum StatusMissao
    {
        Disponivel,
        EmAndamento,
        Concluida
    }

    // Enum para o tipo de item
    public enum TipoItem
    {
        Consumivel,
        Equipamento
    }
}
